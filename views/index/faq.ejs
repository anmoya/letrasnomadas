<% include ../partials/header %>

<div class="container">
    <div class="row">
        <div class="col-12">
            <% faqs.forEach( item => { %>
                <h1><%= item.title %></h1>
                <p><%= item.text %></p>
            <% }) %>
        </div>
    </div>
</div>

<% include ../partials/footer %>